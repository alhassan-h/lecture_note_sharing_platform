# Database Configuration
# Options: 'sqlite' (default), 'mysql', or 'postgres'/'supabase'
DB_CONNECTION=sqlite

# DATABASE_URL (optional — only needed if using non-default settings)
# For SQLite (default location):
# DATABASE_URL=sqlite:///lnsp.db

# For MySQL (example):
# DATABASE_URL=mysql+pymysql://lnsp_user:strongpassword@localhost/lnsp

# For PostgreSQL / Supabase (example):
# DATABASE_URL=postgresql://postgres.xyzabcd:password@db.xyzabcd.supabase.co:5432/postgres

# Supabase Configuration (if using Supabase for file storage)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-role-key
SUPABASE_BUCKET_NAME=lecture-notes

# Flask Secret Key (change this in production!)
SECRET_KEY=dev-secret-key-change-in-production